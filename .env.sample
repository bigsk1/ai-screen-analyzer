# for openai gpt-4o is used for image analysis and chat
# ollama uses llava for image analysis so make sure you have it installed

# API keys are optional. The app will work with any combination of providers.
# Leave keys blank to use simulated responses for that provider.

# OpenAI API key (optional - only needed for GPT models)
REACT_APP_OPENAI_API_KEY=

# Anthropic API key (optional - only needed for Claude models)
ANTHROPIC_API_KEY=

# anthropic model claude-3-5-sonnet-20241022
ANTHROPIC_MODEL=claude-3-7-sonnet-20250219

# Backend API URL (required for Anthropic integration)
# Default is http://localhost:5000 if not specified
# For Docker:
# - If backend is in another container: http://backend:5000 (using Docker Compose service name)
# - If backend is on host: http://host.docker.internal:5000
REACT_APP_BACKEND_URL=http://localhost:5000

# Ollama API URL (required only for Ollama models)
# Default is http://localhost:11434 if not specified
# For Docker:
# - If Ollama is on host: http://host.docker.internal:11434
# - If Ollama is in another container: http://ollama:11434
OLLAMA_API_URL=http://localhost:11434

# Default Ollama image model for image analysis (optional)
# REACT_APP_OLLAMA_IMAGE_MODEL=llava